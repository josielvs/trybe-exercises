let n = 20;
let lineItems;
let asterix = '*';
let line = '';

for (lineItems = 0; lineItems < n; lineItems += 1) {
  line = line + asterix;
};
for (lineItems = 0; lineItems < n; lineItems += 1) {
  console.log(line);
};

//console.log(' '.repeat(zeros--)+ '*')
